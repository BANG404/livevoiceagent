优化下agent 设计，默认一开始传入根据手机号检索的近5次的记录，以  user message 传入给agent，然后让 agent 欢迎并发言，替代掉一开始的 twilio say，以实现 @DOCS/refer.md 的回访场景
删除掉 lookup_recent_visit 工具，以及 calculator 工具，保留 register_visitor ，该工具接入 企业微信机器人，会发送消息给指定的人，且统一使用 sqlite 来保留记录，方便后续的手机号检索记录